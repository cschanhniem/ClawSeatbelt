# Task Log: Release 0.1.1

## Goal

Publish the current `main` branch as `clawseatbelt@0.1.1` through the trusted publishing workflow.

## Scope

- Bump package, lockfile, and plugin manifest to `0.1.1`.
- Update install and publish docs to point at the new release.
- Preserve the local-first deployment path for development and tarball rehearsal.

## Release Notes

- New local deployment commands: `npm run deploy:local` and `npm run deploy:local:pack`
- Trusted publishing workflow is now the preferred release path
- Release workflow now enforces `main` lineage before publish
